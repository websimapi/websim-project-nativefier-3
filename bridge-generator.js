export function generateAndDownloadBridge() {
    const zip = new JSZip();

    const packageJsonContent = `{
  "name": "websim-nativefier-bridge",
  "version": "1.4.0",
  "description": "A local server to build native apps from websim projects.",
  "main": "index.js",
  "scripts": {
    "start:bridge": "node index.js",
    "start:server": "node exe-download-server.js",
    "start": "concurrently -k -n bridge,server -c auto,auto \"npm:start:bridge\" \"npm:start:server\"",
    "postinstall": "npm install --no-optional --ignore-scripts || echo 'Continuing without optional deps'"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "@electron-forge/cli": "^6.4.2",
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.7",
    "ws": "^8.17.1"
  }
}
`;

    const indexJsContent = "const WebSocket = require(\"ws\");const { exec } = require(\"child_process\");const fs = require(\"fs\");const path = require(\"path\");const { rimraf } = require(\"rimraf\");const archiver = require(\"archiver\");const { buildWithForge } = require(\"./forge-builder\");const PORT = 3001;const wss = new WebSocket.Server({ port: PORT });console.log(\"WebSocket bridge listening on ws://localhost:\" + PORT);checkDependencies();wss.on(\"connection\", ws => {console.log(\"Client connected\");ws.on(\"message\", message => {try{const data = JSON.parse(message);if(data.type === \"start_build\"){console.log(\"Received build request:\", data);handleBuild(data, ws);}}catch(error){console.error(\"Failed to parse message:\", error);ws.send(JSON.stringify({ type: \"build_error\", error: \"Invalid request from client.\" }));}});ws.on(\"close\", () => {console.log(\"Client disconnected\");});});function checkDependencies(){console.log(\"Checking system dependencies...\");exec(\"which convert || which gm\", (error)=>{if(error){console.log(\"ImageMagick/GraphicsMagick not found. Installing ImageMagick...\");const installCmd = process.platform === \"linux\" ? \"sudo apt-get update && sudo apt-get install -y imagemagick\" : (process.platform === \"darwin\" ? \"brew install imagemagick\" : \"echo \\\"Please install ImageMagick manually on Windows\\\"\");exec(installCmd,(installError)=>{if(installError){console.warn(\"Failed to auto-install ImageMagick. You may need to install it manually.\");}else{console.log(\"ImageMagick installed successfully.\");}});}else{console.log(\"ImageMagick/GraphicsMagick found.\");}});if(process.platform === \"linux\"){exec(\"which wine\",(error)=>{if(error){console.log(\"Wine not found. Installing wine...\");exec(\"sudo apt-get update && sudo apt-get install -y wine\",(installError)=>{if(installError){console.warn(\"Failed to auto-install Wine. You may need to install it manually.\");}else{console.log(\"Wine installed successfully.\");}});}else{console.log(\"Wine found.\");exec(\"which wine64\",(wine64Error)=>{if(wine64Error){exec(\"sudo ln -sf $(which wine) /usr/local/bin/wine64\",(linkError)=>{if(!linkError){console.log(\"Created wine64 symlink.\");}});}});}});}}function sleep(ms){return new Promise(resolve=>setTimeout(resolve,ms));}function cleanupOldFiles(distDir,maxAgeMs=15*60*1000){fs.readdir(distDir,(err,files)=>{if(err)return;const now=Date.now();files.forEach(file=>{const filePath=path.join(distDir,file);fs.stat(filePath,(err,stats)=>{if(!err && now - stats.mtimeMs > maxAgeMs){fs.unlink(filePath,()=>{});}});});});}function zipDirectory(source,out){const archive=archiver(\"zip\",{ zlib:{ level:9 }});const stream=fs.createWriteStream(out);return new Promise((resolve,reject)=>{archive.directory(source,false).on(\"error\",err=>reject(err)).pipe(stream);stream.on(\"close\",()=>resolve(out));archive.finalize();});}async function handleBuild(options,ws,attempt=1){const { url, platform, arch, appName, requestId } = options;const outputDir=path.join(__dirname,\"builds\");const distDir=path.join(__dirname,\"dist\");if(!fs.existsSync(outputDir)) fs.mkdirSync(outputDir,{ recursive:true });if(!fs.existsSync(distDir)) fs.mkdirSync(distDir,{ recursive:true });const sanitizedAppName=(appName||\"MyApp\").replace(/[^a-zA-Z0-9.-]/g,\"\");const finalAppName=sanitizedAppName||\"MyApp\";let extraFlags=\"\";if(platform===\"windows\" && process.platform === \"linux\"){extraFlags += \" --no-icon --no-win32metadata\";console.log(\"Building Windows app on Linux - skipping icon conversion and win32 metadata\");}const env={ ...process.env };if(platform===\"windows\" && process.platform !== \"win32\"){env.CROSS_SPAWN_WINDOWS_EXE_WINE_COMMAND=\"wine\";env.WINE=\"wine\";env.WINEPREFIX=path.join(__dirname, \".wine\");if(!fs.existsSync(env.WINEPREFIX)){console.log(\"Initializing wine prefix...\");exec(\"winecfg\",{ env },()=>{console.log(\"Wine prefix initialized.\");});}}const command = \"npx @nativefier/nativefier \\\"\" + url + \"\\\" --name \\\"\" + finalAppName + \"\\\" --platform \\\"\" + platform + \"\\\" --arch \\\"\" + arch + \"\\\" --out \\\"\" + outputDir + \"\\\" --overwrite\" + extraFlags;console.log(\"[Attempt \" + attempt + \"] Executing: \" + command);ws.send(JSON.stringify({ type:\"build_progress\", message:\"[Attempt \" + attempt + \"] Build started...\", requestId }));let buildPath=\"\";const buildProcess=exec(command,{ env });buildProcess.stdout.on(\"data\",(data)=>{const line=data.toString();console.log(\"stdout: \" + line.trim());const match=line.match(/App built to (.+?),/);if(match && match[1]){buildPath=match[1].trim();console.log(\"Detected build path from stdout: \" + buildPath);}});buildProcess.stderr.on(\"data\",(data)=>{console.error(\"stderr: \" + data.toString().trim());});buildProcess.on(\"close\",async(code)=>{if(code !== 0){const errorMessage=\"Nativefier process exited with code \" + code + \" (attempt \" + attempt + \")\";console.error(errorMessage);if(attempt < 3){ws.send(JSON.stringify({ type:\"build_progress\", message:\"Build failed, retrying (\" + (attempt+1) + \"/3)...\", requestId }));await sleep(2000);return handleBuild(options, ws, attempt + 1);}else{ws.send(JSON.stringify({ type:\"build_error\", error:errorMessage, requestId }));return;}}console.log(\"Nativefier build completed successfully.\");ws.send(JSON.stringify({ type:\"build_progress\", message:\"Build complete, zipping application...\", requestId }));if(!buildPath || !fs.existsSync(buildPath)){const errorMessage=\"Could not find Nativefier output directory. See logs.\";console.error(errorMessage);if(buildPath) console.error(\"Detected path \\\"\" + buildPath + \"\\\" does not exist.\");ws.send(JSON.stringify({ type:\"build_error\", error:errorMessage, requestId }));return;}const zipName = finalAppName + \"_\" + platform + \"_\" + Date.now() + \".zip\";const zipPath=path.join(distDir, zipName);console.log(\"Zipping directory: \" + buildPath + \" to \" + zipPath);try{await zipDirectory(buildPath, zipPath);console.log(\"Zipping complete.\");ws.send(JSON.stringify({ type:\"build_complete\", fileName: zipName, appName: finalAppName, requestId, downloadUrl: \"/download?file=\" + encodeURIComponent(zipName) }));console.log(\"Sent zip info to client.\");await rimraf(buildPath);console.log(\"Cleaned up build directory: \" + buildPath);setTimeout(()=>{fs.unlink(zipPath,(err)=>{if(!err) console.log(\"Temp zip removed: \" + zipPath);});}, 10*60*1000);cleanupOldFiles(distDir);}catch(err){console.error(\"Zipping or cleanup failed:\", err);ws.send(JSON.stringify({ type:\"build_error\", error:\"Zipping the application failed.\", requestId }));}});} ";

    const exeServerContent = "const http = require(\"http\");const fs = require(\"fs\");const path = require(\"path\");const DIST_DIR = path.join(__dirname, \"dist\");const PORT = 3002;const server = http.createServer((req, res) => {if (req.url.startsWith(\"/download?file=\")) {const fileName = decodeURIComponent(req.url.split(\"=\")[1] || \"\");if (path.normalize(fileName).includes(\"..\") || !fileName.endsWith(\".zip\")) {res.writeHead(400);return res.end(\"Invalid request\");}const filePath = path.join(DIST_DIR, fileName);if (!fs.existsSync(filePath)) {res.writeHead(404);return res.end(\"File not found\");}res.writeHead(200, {\"Content-Type\":\"application/zip\",\"Content-Disposition\":\"attachment; filename=\\\"\" + fileName + \"\\\"\",\"Access-Control-Allow-Origin\":\"*\"});fs.createReadStream(filePath).pipe(res);} else {res.writeHead(404);res.end(\"Not found\");}});server.listen(PORT, \"localhost\", () => {console.log(\"Download server running at http://localhost:\" + PORT);});";

    const forgeBuilderContent = "const { exec } = require(\"child_process\");const fs = require(\"fs\");const path = require(\"path\");const { rimraf } = require(\"rimraf\");function run(cmd, opts = {}) {return new Promise((resolve, reject) => {const p = exec(cmd, { maxBuffer: 1024 * 1024 * 10, ...opts }, (err, stdout, stderr) => {if (err) return reject(err);resolve({ stdout, stderr });});p.stdout && p.stdout.pipe(process.stdout);p.stderr && p.stderr.pipe(process.stderr);});}function mapPlatform(p){if(p === \"windows\") return \"win32\";if(p === \"mac\") return \"darwin\";return \"linux\";}async function buildWithForge(opts, ws){const { url, platform, arch, appName = \"MyApp\", outputDir, distDir, requestId } = opts;const safeName = (appName || \"MyApp\").replace(/[^a-zA-Z0-9.-]/g,\"\") || \"MyApp\";const workDir = path.join(outputDir, safeName + \"-\" + Date.now());const srcDir = path.join(workDir, \"src\");const forgePlatform = mapPlatform(platform);fs.mkdirSync(srcDir, { recursive:true });fs.mkdirSync(distDir, { recursive:true });const pkg = {name: safeName.toLowerCase(),productName: safeName,version: \"1.0.0\",main: \"src/main.js\",private: true,scripts: {start: \"electron-forge start\",package: \"electron-forge package\",make: \"electron-forge make\",publish: \"electron-forge publish\"},config: {makers: [{ name: \"@electron-forge/maker-zip\" }]},devDependencies: {electron: \"^30.0.0\",\"@electron-forge/cli\": \"^6.4.2\",\"@electron-forge/maker-zip\": \"^6.4.2\"}};const mainJs = \"const { app, BrowserWindow } = require(\\\"electron\\\");function createWindow(){const win = new BrowserWindow({ width:1280, height:800, webPreferences:{ nodeIntegration:false, contextIsolation:true }});win.setMenu(null);win.loadURL(\\\"" + url.replace(/"/g, '\\\\"') + "\\\");}app.whenReady().then(createWindow);app.on(\\\"window-all-closed\\\",()=>{ if(process.platform !== \\\"darwin\\\") app.quit(); });\";fs.writeFileSync(path.join(workDir, \"package.json\"), JSON.stringify(pkg, null, 2));fs.writeFileSync(path.join(srcDir, \"main.js\"), mainJs);ws && ws.send(JSON.stringify({ type:\"build_progress\", message:\"Installing app dependencies...\", requestId }));await run(\"npm install --omit=optional\", { cwd: workDir });ws && ws.send(JSON.stringify({ type:\"build_progress\", message:\"Packaging with Electron Forge...\", requestId }));await run(\"npx --yes @electron-forge/cli@6 make --platform \" + forgePlatform + \" --arch \" + arch, { cwd: workDir });const makeDir = path.join(workDir, \"out\", \"make\");if(!fs.existsSync(makeDir)) throw new Error(\"Forge output not found\");let foundZip = null;const walk = dir => {for (const f of fs.readdirSync(dir)){const fp = path.join(dir, f);const stat = fs.statSync(fp);if(stat.isDirectory()) walk(fp);else if(f.endsWith(\".zip\")) foundZip = foundZip || fp;}};walk(makeDir);if(!foundZip) throw new Error(\"No zip artifact produced by Electron Forge\");const outName = safeName + \"_\" + platform + \"_\" + Date.now() + \".zip\";const dest = path.join(distDir, outName);fs.copyFileSync(foundZip, dest);await rimraf(workDir);return { zipPath: dest, fileName: outName };}module.exports = { buildWithForge };";

    zip.file("package.json", packageJsonContent);
    zip.file("index.js", indexJsContent);
    zip.file("exe-download-server.js", exeServerContent);
    zip.file("forge-builder.js", forgeBuilderContent);
    zip.folder("builds");
    zip.folder("dist");

    zip.generateAsync({ type: "blob" })
        .then(function(content) {
            const link = document.createElement('a');
            link.href = URL.createObjectURL(content);
            link.download = "websim-bridge.zip";
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        });
}